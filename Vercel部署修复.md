# Vercel 部署修复指南

## 🔍 问题说明

错误：`No Next.js version detected`

这通常是因为：
1. Vercel 的 Root Directory 设置不正确
2. package.json 文件不在正确的位置
3. 项目结构问题

## ✅ 解决方案

### 方法一：检查 Vercel 设置（推荐）

#### 1. 检查项目结构

确保你的项目结构是这样的：

```
website/
├── package.json      ← 必须在根目录
├── next.config.js
├── app/
├── components/
└── ...
```

#### 2. 在 Vercel 中检查设置

1. 进入 Vercel 项目设置
2. 点击 **Settings** → **General**
3. 找到 **Root Directory** 设置
4. **确保设置为空或 `./`**（不要设置子目录）
5. 保存设置

#### 3. 重新部署

点击 **Deployments** → 找到失败的部署 → 点击 **Redeploy**

---

### 方法二：检查 package.json

确保 `package.json` 在项目根目录，并且包含 `next` 依赖：

```json
{
  "dependencies": {
    "next": "14.0.4",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
```

---

### 方法三：使用 vercel.json 配置（如果需要）

如果项目在子目录，创建 `vercel.json`：

```json
{
  "buildCommand": "cd website && npm install && npm run build",
  "outputDirectory": "website/.next",
  "installCommand": "cd website && npm install"
}
```

**但通常不需要这个，因为项目应该在根目录。**

---

## 🚀 正确的部署步骤

### 1. 确保项目结构正确

```
website/
├── package.json          ← 必须存在
├── next.config.js        ← 必须存在
├── app/                  ← Next.js 应用目录
├── components/
├── lib/
└── ...
```

### 2. 在 Vercel 中导入项目

1. 访问 https://vercel.com/
2. 点击 **Add New Project**
3. 选择你的 GitHub 仓库
4. **重要**：检查以下设置：
   - **Framework Preset**: Next.js（应该自动检测）
   - **Root Directory**: 留空或设置为 `./`
   - **Build Command**: 留空（使用默认）
   - **Output Directory**: 留空（使用默认）
5. 添加环境变量：
   - `CONTENTFUL_SPACE_ID`
   - `CONTENTFUL_ACCESS_TOKEN`
6. 点击 **Deploy**

---

## 🔧 常见问题排查

### 问题 1：Root Directory 设置错误

**症状**：Vercel 找不到 package.json

**解决**：
1. 进入 Vercel 项目设置
2. Settings → General → Root Directory
3. 设置为空或 `./`
4. 重新部署

### 问题 2：package.json 不在根目录

**症状**：项目在子目录中

**解决**：
- 选项 A：将项目移到仓库根目录
- 选项 B：在 Vercel 中设置 Root Directory 为项目目录名

### 问题 3：依赖未安装

**症状**：构建失败

**解决**：
1. 确保 `package.json` 中有所有依赖
2. 确保 `package-lock.json` 已提交到 Git
3. 在 Vercel 设置中检查 Install Command

---

## ✅ 验证清单

部署前检查：

- [ ] `package.json` 在项目根目录
- [ ] `package.json` 包含 `next` 依赖
- [ ] `next.config.js` 存在
- [ ] `app/` 目录存在（Next.js 13+ App Router）
- [ ] Vercel Root Directory 设置为空或 `./`
- [ ] 环境变量已配置
- [ ] 代码已推送到 GitHub

---

## 🎯 快速修复步骤

1. **检查 Vercel 设置**：
   - Root Directory: 留空
   - Framework: Next.js

2. **检查项目结构**：
   ```bash
   cd D:\project\website
   ls package.json  # 应该存在
   ```

3. **重新部署**：
   - 在 Vercel 中点击 Redeploy

---

## 📝 如果仍然失败

### 查看构建日志

1. 在 Vercel 中打开失败的部署
2. 查看 **Build Logs**
3. 找到具体错误信息

### 本地测试构建

```bash
cd D:\project\website
npm install
npm run build
```

如果本地构建成功，但 Vercel 失败，通常是配置问题。

---

## 💡 推荐配置

在 Vercel 项目设置中：

- **Framework Preset**: Next.js
- **Root Directory**: （留空）
- **Build Command**: （留空，使用默认）
- **Output Directory**: （留空，使用默认）
- **Install Command**: （留空，使用默认）
- **Node.js Version**: 18.x 或 20.x

---

## 🆘 需要帮助？

如果问题仍然存在：
1. 检查 Vercel 构建日志
2. 确认项目结构正确
3. 确认 package.json 在根目录
4. 检查 Root Directory 设置

