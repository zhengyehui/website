# 快速修复 Git 推送问题

## 🔧 问题 1：Permission denied (publickey)

这是因为使用了 SSH 方式，但没有配置 SSH 密钥。

## 🔧 问题 2：error: src refspec master does not match any

这是因为本地还没有创建 `master` 分支或还没有进行任何提交。

## ✅ 解决方案：改用 HTTPS（最简单）

### 步骤 1：修改远程地址

在项目目录运行：

```bash
# 删除旧的远程地址
git remote remove origin

# 添加 HTTPS 地址（替换为你的实际地址）
git remote add origin https://github.com/你的用户名/仓库名.git
```

**示例**：
```bash
git remote remove origin
git remote add origin https://github.com/zhangsan/website.git
```

### 步骤 2：重新推送

```bash
# 确保分支名称是 master
git branch -M master

# 推送
git push -u origin master
```

### 步骤 3：输入凭据

如果提示输入用户名和密码：
- **用户名**：你的 GitHub 用户名
- **密码**：使用 **Personal Access Token**（不是 GitHub 密码）

---

## 🔑 获取 Personal Access Token

### 1. 创建 Token

1. 访问：https://github.com/settings/tokens
2. 点击 "Generate new token" → "Generate new token (classic)"
3. 填写：
   - Note: `website-deploy`
   - 勾选 `repo` 权限
   - 点击 "Generate token"
4. **立即复制 Token**（只显示一次！）

### 2. 使用 Token

推送时：
- 用户名：你的 GitHub 用户名
- 密码：粘贴刚才复制的 Token

---

## 🚀 完整命令（复制粘贴即可）

### 如果是 "src refspec master does not match any" 错误

```bash
# 进入项目目录
cd D:\project\website

# 初始化 Git（如果还没初始化）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 确保分支是 master
git branch -M master

# 添加远程仓库（如果还没添加）
git remote add origin https://github.com/zhengyehui/website.git

# 推送
git push -u origin master
```

### 如果是 "Permission denied" 错误

```bash
# 进入项目目录
cd D:\project\website

# 修改远程地址（替换为你的地址）
git remote remove origin
git remote add origin https://github.com/zhengyehui/website.git

# 确保分支是 master
git branch -M master

# 推送
git push -u origin master
```

---

## 📝 或者使用一键脚本（推荐）

运行 `一键推送.bat`，自动完成所有步骤！

或者运行 `修复Git推送.bat`，按提示操作。

---

## ⚠️ 如果 Git 命令找不到

刷新环境变量：

```powershell
$env:Path = [System.Environment]::GetEnvironmentVariable("Path","Machine") + ";" + [System.Environment]::GetEnvironmentVariable("Path","User")
```

或者重启终端。

---

## ✅ 完成后

推送成功后，就可以在 Vercel 中导入 GitHub 仓库了！

