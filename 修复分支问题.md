# 修复 "src refspec master does not match any" 错误

## 🔍 问题说明

这个错误表示：
- 本地还没有创建 `master` 分支
- 或者还没有进行任何提交

## ✅ 解决方案

### 步骤 1：进入项目目录

```bash
cd D:\project\website
```

### 步骤 2：检查 Git 状态

```bash
# 查看当前分支
git branch

# 查看提交历史
git log
```

### 步骤 3：初始化并提交（如果还没做）

```bash
# 如果还没初始化 Git
git init

# 添加所有文件
git add .

# 创建第一次提交
git commit -m "Initial commit"
```

### 步骤 4：创建并切换到 master 分支

```bash
# 如果当前分支是 master，重命名为 master
git branch -M master

# 或者如果还没有分支，创建 master 分支
git checkout -b master
```

### 步骤 5：推送到 GitHub

```bash
# 推送到 master 分支
git push -u origin master
```

---

## 🚀 完整命令（一键执行）

```bash
cd D:\project\website

# 初始化（如果还没初始化）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit"

# 确保分支是 master
git branch -M master

# 添加远程仓库（如果还没添加）
git remote add origin https://github.com/zhengyehui/website.git

# 推送
git push -u origin master
```

---

## ⚠️ 如果仍然失败

### 检查是否在正确的目录

确保你在 `D:\project\website` 目录下，而不是 `D:\project` 目录。

### 检查是否有文件需要提交

```bash
# 查看状态
git status

# 如果有未跟踪的文件，添加它们
git add .

# 然后提交
git commit -m "Initial commit"
```

### 检查远程仓库是否正确

```bash
# 查看远程地址
git remote -v

# 如果不对，删除后重新添加
git remote remove origin
git remote add origin https://github.com/zhengyehui/website.git
```

---

## 📝 常见情况

### 情况 1：还没有初始化 Git

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M master
git remote add origin https://github.com/zhengyehui/website.git
git push -u origin master
```

### 情况 2：已经初始化，但分支是 master

```bash
git branch -M master
git push -u origin master
```

### 情况 3：已经有提交，但分支名不对

```bash
# 查看当前分支
git branch

# 如果是 master，重命名为 master
git branch -M master

# 推送
git push -u origin master
```

---

## ✅ 验证

推送成功后，你应该看到类似这样的输出：

```
Enumerating objects: X, done.
Counting objects: 100% (X/X), done.
Writing objects: 100% (X/X), done.
To https://github.com/zhengyehui/website.git
 * [new branch]      master -> master
Branch 'master' set up to track remote branch 'master' from 'origin'.
```

然后在 GitHub 上刷新你的仓库页面，应该能看到所有文件了！

