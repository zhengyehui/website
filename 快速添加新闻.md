# 快速添加新闻指南

## 🎯 两种添加新闻的方式

### 方式一：直接在代码中添加（最简单，适合少量新闻）

#### 步骤 1：编辑 `app/page.tsx`

找到这一行：
```typescript
const posts: Post[] = [];
```

替换为：
```typescript
const posts: Post[] = [
  {
    fields: {
      title: '第一条新闻标题',
      slug: 'first-news',
      excerpt: '这是新闻摘要',
      publishedAt: '2024-12-17',
    },
  },
  {
    fields: {
      title: '第二条新闻标题',
      slug: 'second-news',
      excerpt: '这是第二条新闻的摘要',
      publishedAt: '2024-12-18',
    },
  },
];
```

#### 步骤 2：编辑 `app/news/page.tsx`

找到这一行：
```typescript
const posts: Post[] = [];
```

替换为同样的数组（或更多新闻）。

#### 步骤 3：推送到 GitHub

```bash
git add .
git commit -m "添加新闻"
git push origin main
```

### 方式二：使用 Contentful（推荐，适合经常更新）

#### 步骤 1：配置 Contentful

1. 注册 Contentful：https://www.contentful.com/
2. 创建空间（Space）
3. 创建内容类型（Content Model）：
   - 名称：Post
   - 字段：
     - `title` (Short text, required)
     - `slug` (Short text, required, unique)
     - `excerpt` (Long text)
     - `publishedAt` (Date & time)
     - `content` (Rich text)
4. 获取 API Key

#### 步骤 2：配置环境变量

在 `.env.local` 中：
```env
CONTENTFUL_SPACE_ID=你的-space-id
CONTENTFUL_ACCESS_TOKEN=你的-access-token
```

在 Vercel 中也添加这些环境变量。

#### 步骤 3：恢复代码

在 `app/page.tsx` 中：
```typescript
import { getPosts } from '@/lib/contentful';

export default async function Home() {
  const posts: Post[] = await getPosts(3);
  // 删除或注释：const posts: Post[] = [];
  
  return (
    <div>
      <Hero />
      <About />
      <Services />
      <News posts={posts} />
    </div>
  );
}
```

#### 步骤 4：在 Contentful 中添加内容

1. 登录 Contentful 后台
2. 点击 "Content" → "Add entry"
3. 选择 Post 类型
4. 填写内容
5. 点击 "Publish"

## 💡 推荐

**对于小公司，推荐方式一（代码方式）**：
- ✅ 简单直接
- ✅ 不需要额外配置
- ✅ 适合少量新闻
- ✅ 完全控制

**如果新闻很多或经常更新，使用方式二（Contentful）**：
- ✅ 非技术人员也能更新
- ✅ 不需要改代码
- ✅ 适合频繁更新

## 📝 示例：添加 3 条新闻

```typescript
const posts: Post[] = [
  {
    fields: {
      title: '公司成立',
      slug: 'company-established',
      excerpt: '我们公司正式成立了！感谢大家的支持。',
      publishedAt: '2024-01-01',
    },
  },
  {
    fields: {
      title: '新产品发布',
      slug: 'new-product-launch',
      excerpt: '我们发布了全新的产品，欢迎大家体验。',
      publishedAt: '2024-01-15',
    },
  },
  {
    fields: {
      title: '获得行业认证',
      slug: 'industry-certification',
      excerpt: '我们获得了行业权威认证，这是对我们专业能力的认可。',
      publishedAt: '2024-02-01',
    },
  },
];
```

## ✅ 完成后

1. 保存文件
2. 推送到 GitHub
3. Vercel 自动部署
4. 访问网站查看新闻

