# Contentful 配置步骤（图文版）

## 🎯 目标

在 Contentful 中创建 Post 内容类型，让非技术人员也能更新新闻。

---

## 第一步：登录并进入 Content Model

1. 访问：https://www.contentful.com/
2. 登录你的账号
3. 点击左侧菜单 **Content model**

---

## 第二步：创建 Post 内容类型

### 2.1 点击创建

1. 点击右上角 **Add content type** 按钮
2. 或点击 **Create content type**

### 2.2 填写基本信息

在弹出窗口中：

- **Name**: 输入 `Post`
- **API Identifier**: 会自动填充为 `post`（确保是小写）
- **Description**: `新闻和文章内容`（可选）

点击 **Create**

---

## 第三步：添加字段

### 字段 1：Title（标题）

1. 点击 **Add field** 按钮
2. 选择 **Short text**
3. 填写：
   - **Name**: `Title`
   - **Field ID**: `title`（自动生成，确保是这个）
   - **Required field**: ✅ **勾选**（重要！）
4. 点击 **Create and configure**
5. 点击 **Create**

### 字段 2：Slug（URL标识）

1. 再次点击 **Add field**
2. 选择 **Short text**
3. 填写：
   - **Name**: `Slug`
   - **Field ID**: `slug`
   - **Required field**: ✅ **勾选**
   - **Unique field**: ✅ **勾选**（重要！）
4. 点击 **Create and configure**
5. 点击 **Create**

### 字段 3：Excerpt（摘要）

1. 点击 **Add field**
2. 选择 **Long text**
3. 填写：
   - **Name**: `Excerpt`
   - **Field ID**: `excerpt`
   - **Required field**: ❌ **不勾选**
4. 点击 **Create and configure**
5. 点击 **Create**

### 字段 4：Published At（发布时间）

1. 点击 **Add field**
2. 选择 **Date & time**
3. 填写：
   - **Name**: `Published At`
   - **Field ID**: `publishedAt`（注意大小写）
   - **Required field**: ❌ **不勾选**
4. 点击 **Create and configure**
5. 点击 **Create**

### 字段 5：Content（正文，可选）

1. 点击 **Add field**
2. 选择 **Rich text**
3. 填写：
   - **Name**: `Content`
   - **Field ID**: `content`
   - **Required field**: ❌ **不勾选**
4. 点击 **Create and configure**
5. 点击 **Create**

### 保存内容类型

点击右上角 **Save** 按钮

---

## 第四步：添加第一条新闻

### 4.1 进入内容管理

1. 点击左侧菜单 **Content**
2. 点击右上角 **Add entry** 按钮
3. 选择 **Post**

### 4.2 填写内容

在编辑器中填写：

- **Title**: `欢迎来到我们的网站`
- **Slug**: `welcome`（注意：小写，无空格）
- **Excerpt**: `这是第一条新闻，欢迎访问我们的网站！`
- **Published At**: 点击日期选择器，选择今天
- **Content**: 输入文章正文（可选）

### 4.3 发布

1. 点击右上角 **Publish** 按钮
2. 确认发布

---

## 第五步：验证配置

### 5.1 获取 API Key

1. 点击左侧菜单 **Settings**
2. 点击 **API keys**
3. 复制：
   - **Space ID**
   - **Content delivery API - access token**

### 5.2 配置环境变量

在 `.env.local` 中：

```env
CONTENTFUL_SPACE_ID=你的-space-id
CONTENTFUL_ACCESS_TOKEN=你的-access-token
```

### 5.3 在代码中测试

在 `app/page.tsx` 中取消注释：

```typescript
import { getPosts } from '@/lib/contentful';

export default async function Home() {
  const posts: Post[] = await getPosts(3);
  // 删除：const posts: Post[] = [];
  
  return (
    <div>
      <Hero />
      <About />
      <Services />
      <News posts={posts} />
    </div>
  );
}
```

### 5.4 测试

运行 `npm run dev`，访问首页，应该能看到新闻了！

---

## 📋 快速检查清单

- [ ] 已创建 Post 内容类型
- [ ] API Identifier 是 `post`（小写）
- [ ] 已添加 title 字段（必填）
- [ ] 已添加 slug 字段（必填、唯一）
- [ ] 已添加 excerpt 字段（可选）
- [ ] 已添加 publishedAt 字段（可选）
- [ ] 已添加至少一条新闻
- [ ] 新闻已发布（Published）
- [ ] 已配置环境变量
- [ ] 代码中已恢复 Contentful 调用

---

## 🎯 完成后的效果

- ✅ 可以在 Contentful 后台添加新闻
- ✅ 网站自动显示最新新闻
- ✅ 非技术人员也能更新内容
- ✅ 无需修改代码

---

**配置完成后，就可以轻松管理网站内容了！**

